class Greet {
    Greet(who) {
        this.name = who.toUpperCase().charAt(0) +
            who.substring(1);
    }

    void salute() {
        print("Hello " + name + "!");
    }
}

g = new Greet("world"); // create object
g.salute(); // output "Hello World!"